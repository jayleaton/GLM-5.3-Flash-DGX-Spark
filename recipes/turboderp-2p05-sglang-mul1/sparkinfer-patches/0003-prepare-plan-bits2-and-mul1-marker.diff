diff -u -r a/sparkinfer/moe/_shared/execution.py b/sparkinfer/moe/_shared/execution.py
--- a/sparkinfer/moe/_shared/execution.py	2026-07-30 07:13:57
+++ b/sparkinfer/moe/_shared/execution.py	2026-09-12 16:19:08
@@ -300,8 +300,8 @@
         )
         if self.source_format == "exl3_trellis_mcg":
             bits = 3 if self.trellis_bits is None else int(self.trellis_bits)
-            if bits not in (3, 4, 5, 6):
-                raise ValueError(f"trellis_bits must be one of 3, 4, 5, 6; got {bits}")
+            if bits not in (2, 3, 4, 5, 6):
+                raise ValueError(f"trellis_bits must be one of 2, 3, 4, 5, 6; got {bits}")
             tile_config = self.trellis_tile_config or (64, 256, 64, 256)
             tile_config = tuple(int(value) for value in tile_config)
             if len(tile_config) != 4 or any(
diff -u -r a/sparkinfer/moe/_shared/kernels/w4a16/prepare.py b/sparkinfer/moe/_shared/kernels/w4a16/prepare.py
--- a/sparkinfer/moe/_shared/kernels/w4a16/prepare.py	2026-07-30 07:13:57
+++ b/sparkinfer/moe/_shared/kernels/w4a16/prepare.py	2026-09-12 16:19:08
@@ -1545,10 +1545,10 @@
             f"{tensor.dtype} tile width"
         )
     bits = last // words_per_bit
-    if bits not in (3, 4, 5, 6):
+    if bits not in (2, 3, 4, 5, 6):
         raise ValueError(
             f"trellis3_t256 {name} encodes unsupported {bits}-bpw storage; "
-            "expected 3, 4, 5, or 6"
+            "expected 2, 3, 4, 5, or 6"
         )
     return bits
 
@@ -1640,13 +1640,14 @@
         raise ValueError("trellis3_t256 W4A16 weights require fp16 or bf16 activations")
     requested_trellis_bits = None if trellis_bits is None else int(trellis_bits)
     if requested_trellis_bits is not None and requested_trellis_bits not in (
+        2,
         3,
         4,
         5,
         6,
     ):
         raise ValueError(
-            "trellis3_t256 bits must be one of 3, 4, 5, or 6, "
+            "trellis3_t256 bits must be one of 2, 3, 4, 5, or 6, "
             f"got {requested_trellis_bits}"
         )
     if num_experts <= 0:
diff -u -r a/sparkinfer/moe/fused_moe/_impl.py b/sparkinfer/moe/fused_moe/_impl.py
--- a/sparkinfer/moe/fused_moe/_impl.py	2026-07-30 07:13:57
+++ b/sparkinfer/moe/fused_moe/_impl.py	2026-09-12 16:19:08
@@ -2564,8 +2564,8 @@
                     "full-rotation Trellis requires hidden and intermediate "
                     "dimensions divisible by 128"
                 )
-            if int(trellis_bits) not in (3, 4, 5, 6):
-                raise ValueError("trellis_bits must be one of 3, 4, 5, 6")
+            if int(trellis_bits) not in (2, 3, 4, 5, 6):
+                raise ValueError("trellis_bits must be one of 2, 3, 4, 5, 6")
             trellis_tile_config = trellis_tile_config or (64, 256, 64, 256)
         routed_capacity = max(int(routed_rows), 1)
         fc1_cols = _activation_w1_rows(activation, int(n))
@@ -4856,6 +4856,7 @@
     intermediate_rotations: torch.Tensor | None = None,
     down_svh: torch.Tensor | None = None,
     trellis_mcg: torch.Tensor | int | None = None,
+    trellis_mul1: torch.Tensor | int | None = None,
     dummy_scale: torch.Tensor | None = None,
 ) -> SPARKINFERFP4ExpertWeights:
     """Transfer source tensors into the planner-selected runtime owner."""
@@ -4873,19 +4874,24 @@
     if plan.source_format == "exl3_trellis_mcg":
         if plan.quant_modes != frozenset({"w4a16"}):
             raise ValueError("EXL3 Trellis weights support only quant_mode='w4a16'")
-        if trellis_mcg is None:
+        if (trellis_mcg is None) == (trellis_mul1 is None):
             raise ValueError(
-                "EXL3 Trellis preparation requires the checkpoint's trellis_mcg "
-                "marker; the runtime decoder is MCG-only and must fail closed"
+                "EXL3 Trellis preparation requires exactly one checkpoint marker, "
+                "trellis_mcg or trellis_mul1; the decoder is specialized per codebook "
+                "and must fail closed"
             )
+        marker_tensor = trellis_mcg if trellis_mcg is not None else trellis_mul1
         marker = (
-            int(trellis_mcg.item())
-            if isinstance(trellis_mcg, torch.Tensor)
-            else int(trellis_mcg)
+            int(marker_tensor.item())
+            if isinstance(marker_tensor, torch.Tensor)
+            else int(marker_tensor)
         ) & 0xFFFFFFFF
-        if marker != 0xCBAC1FED:
+        trellis_codebook = "mcg" if trellis_mcg is not None else "mul1"
+        expected_marker = {"mcg": 0xCBAC1FED, "mul1": 0x83DCD12D}[trellis_codebook]
+        if marker != expected_marker:
             raise ValueError(
-                f"unexpected EXL3 MCG marker {marker:#010x}; expected 0xcbac1fed"
+                f"unexpected EXL3 {trellis_codebook} marker {marker:#010x}; "
+                f"expected {expected_marker:#010x}"
             )
         if not all(
             value is not None
@@ -4945,7 +4951,7 @@
             w13_layout="trellis3_t256_proj",
             trellis_bits=plan.trellis_bits,
             dummy_scale=dummy_scale,
-            codebook="mcg",
+            codebook=trellis_codebook,
             gate_suh=gate_suh,
             up_suh=up_suh,
             intermediate_rotations=intermediate_rotations,
